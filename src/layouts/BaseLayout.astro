---
import "../styles/global.css";

const {
  title = "饥荒联机版食谱大全",
  description = "《饥荒联机版》81 道烹饪锅料理速查。",
} = Astro.props;

const base = import.meta.env.BASE_URL;
const currentPath = Astro.url.pathname.replace(/\/+$/, "") || "/";
const basePath = base.replace(/\/+$/, "") || "/";
const links = [
  {
    href: base,
    label: "总览",
    active: currentPath === basePath,
  },
  {
    href: `${base}recipes/`,
    label: "料理食谱",
    active: currentPath === `${basePath === "/" ? "" : basePath}/recipes`,
  },
  {
    href: `${base}farming/`,
    label: "巨大化配比",
    active: currentPath.includes("/farming"),
  },
  {
    href: `${base}skills/`,
    label: "角色技能树",
    active: currentPath.includes("/skills"),
  },
];
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="theme-color" content="#171914" />
    <meta name="description" content={description} />
    <meta name="generator" content={Astro.generator} />
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <link
      rel="icon"
      type="image/png"
      href={`${base}images/characters/wilson.png`}
      sizes="256x256"
    />
    <link
      rel="apple-touch-icon"
      href={`${base}images/characters/wilson.png`}
    />
    <title>{title}</title>
  </head>
  <body class:list={{ "home-page": currentPath === basePath }}>
    <a class="skip-link" href="#main">跳到主要内容</a>
    <header class="site-nav">
      <nav class="site-nav-inner" aria-label="主导航">
        <div class="site-links">
          {
            links.map((link) => (
              <a
                href={link.href}
                class:list={{ active: link.active }}
                aria-current={link.active ? "page" : undefined}
              >
                {link.label}
              </a>
            ))
          }
        </div>
      </nav>
    </header>
    <slot />
  </body>
</html>
